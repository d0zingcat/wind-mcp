services:
  wind-mcp:
    build: .
    ports:
      - "${PORT:-8888}:8888"
    environment:
      WIND_USE_PROXY: "1"
      WIND_API_URL: "${WIND_API_URL:?set WIND_API_URL to your Wind HTTP gateway, e.g. http://192.168.0.124:6668}"
      PORT: "8888"
    restart: unless-stopped
